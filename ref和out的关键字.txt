
1.他们俩都是传递地址的，所以在传入方法的时候，会改变原值
  其实他们就像c/c++里的指针或是引用类型,因为c#是托管的，所以要实现在传参的时候能够像指针那样，所以才出现了ref/out关键字,

2.ref/out 在调用函数的时候都要显性出来

3.ref/out：区别在于: 
	ref的实参要初始化,并且值可以带入到函数里面(所谓可入可出)
	out的实参无需初始化，并且值带不进去(所谓只出不可进),在进入函数的时候，out参数会归无，如果没有在函数里赋值使用的话，会像刚出生的变量一样出问题:使用了未赋值的值